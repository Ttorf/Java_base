	Задание на API.

Необходимо написать клиента для работы со сервисом https://swapi.co/

Все классы описания данных должны находится в папке models
Все классы с манипуляциями данных должны находится в папке utils

Клиент должен реализовывать интерфейс, в  котором описаны следующие методы:
•	getPeopleByName (String name);
•	getPeopleByID(int id) ;
•	getAllPeople() ;
•
•	getPlanetByName(String name) ;
•	getPlanetByID(int id) ;
•	getAllPlanets() ;
•
•	getFilmByTitle(String title) ;
•	getFilmByID(int id) ;
•	getAllFilms() ;
•
•	getSpeciesByName(String name) ;
•	getSpeciesByID(int id) ;
•	getAllSpecies() ;
•
•	getVehicleByName(String name) ;
•	getVehicleByID(int id) ;
•	getAllVehicles() ;
•
•	getStarshipByName(String name) ;
•	getStarshipByID(int id) ;
•	getAllStarships() ;

Помимо этого у клиента должны быть функции:
•	getByUrl(String url, Type t)
•	getAllPage(String url, Type t)
•	getOnePage(String jsonStr, Type type)

Которые могут работать с любыми объектами с помощью Generic

Все методы которые по работе со страницей должны вынесены в отдельный класс.






Клиент

